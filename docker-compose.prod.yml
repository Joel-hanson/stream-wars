version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"  # Next.js app
      - "3001:3001"  # WebSocket server
    # volumes:
      # Mount certificates directory (read-only)
      # Uncomment if using file-based certificates
      # - ./certs:/app/certs:ro
    environment:
      - NODE_ENV=production
      - KAFKA_BROKERS=kafka:9092
      - NEXT_PUBLIC_WS_URL=ws://localhost:3001
      - WS_PORT=3001
      # Kafka security configuration
      - KAFKA_SSL=${KAFKA_SSL:-false}
      - KAFKA_SASL_MECHANISM=${KAFKA_SASL_MECHANISM:-plain}
      - KAFKA_USERNAME=${KAFKA_USERNAME:-}
      - KAFKA_PASSWORD=${KAFKA_PASSWORD:-}
      # SSL/TLS certificates (uncomment if using)
      # - KAFKA_SSL_CA_PATH=${KAFKA_SSL_CA_PATH:-}
      # - KAFKA_SSL_CERT_PATH=${KAFKA_SSL_CERT_PATH:-}
      # - KAFKA_SSL_KEY_PATH=${KAFKA_SSL_KEY_PATH:-}
      # - KAFKA_SSL_KEY_PASSPHRASE=${KAFKA_SSL_KEY_PASSPHRASE:-}
      # - KAFKA_SSL_REJECT_UNAUTHORIZED=${KAFKA_SSL_REJECT_UNAUTHORIZED:-true}
    depends_on:
      - kafka
      - redis
    restart: unless-stopped

  kafka:
    image: apache/kafka:latest
    hostname: kafka
    container_name: kafka
    ports:
      - "9092:9092"
      - "9093:9093"
    environment:
      # KRaft mode configuration
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      # Topic configuration
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      # Log configuration
      KAFKA_LOG_DIRS: /tmp/kraft-combined-logs
      # Auto create topics
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: 'true'
    restart: unless-stopped

  kafka-ui:
    image: provectuslabs/kafka-ui:latest
    container_name: kafka-ui
    depends_on:
      - kafka
    ports:
      - "8080:8080"
    environment:
      KAFKA_CLUSTERS_0_NAME: local
      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafka:9092
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: redis
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data:
